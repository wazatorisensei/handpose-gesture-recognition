import ReactDOM from 'react-dom/client';
import { HandposeGestureRecognition } from './HandposeGestureRecognition';
import './global.css'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <HandposeGestureRecognition />,
)
